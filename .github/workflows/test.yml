name: Run CI tests

on:
  - push
  - pull_request

jobs:
  build:
    name: clingox ${{ matrix.python-version }} ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: ['ubuntu-latest', 'macos-latest', 'windows-latest']
        python-version: ['3.6', '3.9']

    steps:
    - uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python-version }}

    - name: install prerequisites
      shell: pwsh
      run: pip install --extra-index-url https://test.pypi.org/simple/ clingo clingo-dl clingcon flake8 mypy pylint coverage --upgrade

    - name: install dataclases
      if: ${{ matrix.python-version == '3.6' }}
      shell: pwsh
      run: pip install dataclasses types-dataclasses

    - name: info
      shell: pwsh
      run: python -VV

    - name: test
      shell: pwsh
      run: coverage run -m unittest discover -v

    - name: type check
      if: ${{ matrix.python-version != '3.6' }}
      shell: pwsh
      run: mypy -p clingox

    - name: lint (pylint)
      shell: pwsh
      run: pylint clingox

    - name: lint (flake8)
      shell: pwsh
      run: flake8

    - name: coverage
      shell: pwsh
      run: coverage report --fail-under=100
